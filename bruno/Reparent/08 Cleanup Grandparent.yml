info:
  name: Cleanup Grandparent
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/api/entity/{{reparentGrandparentId}}"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
