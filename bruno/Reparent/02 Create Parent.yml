info:
  name: Create Parent
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/entity"
  headers:
    - name: content-type
      value: application/json
    - name: accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Parent Box",
        "location": {{reparentGrandparentId}},
        "metadata": { "quantity": 1, "owners": [], "tags": [], "islabeled": false }
      }
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status === 200) {
          bru.setVar("reparentParentId", res.body);
        }
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Returns ID as string", function() {
          expect(res.body).to.be.a("string");
        });
