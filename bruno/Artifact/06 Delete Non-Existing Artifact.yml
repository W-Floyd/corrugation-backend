info:
  name: Delete Non-Existing Artifact
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/artifact/9999"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 204 for non-existing artifact", function() {
          expect(res.status).to.equal(204);
        });
