info:
  name: 03 Delete Entity for QR
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/entity/{{qrEntityId}}"
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
