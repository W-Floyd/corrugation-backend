info:
  name: Connect and Receive Update
  type: websocket
  seq: 1

websocket:
  url: "{{wsUrl}}/ws"
  message:
    type: json
    data: "{}"
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
