info:
  name: 22 Check First Available After Deleting A
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/api/entity/find/firstid"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        // State: entity 1 (unlabeled), B (labeled)
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("firstAvailableId is 1 (entity 1 still unlabeled)", function() {
          expect(res.body).to.equal(1);
        });
