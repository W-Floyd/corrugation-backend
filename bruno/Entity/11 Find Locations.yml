info:
  name: Find Locations
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/entity/find/locations"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Returns exactly World (0) as the only location", function() {
          expect(res.body).to.deep.equal([0]);
        });
