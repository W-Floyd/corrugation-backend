info:
  name: Get Entity Contains
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/entity/{{entityId}}/contains"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Returns null (no children)", function() {
          expect(res.body).to.be.null;
        });
