info:
  name: Get Next ID
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/entity/find/nextid"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Next ID is 2", function() {
          expect(res.body).to.equal(2);
        });
