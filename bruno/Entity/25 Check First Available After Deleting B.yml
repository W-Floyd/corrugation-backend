info:
  name: 25 Check First Available After Deleting B
  type: http
  seq: 25

http:
  method: GET
  url: "{{baseUrl}}/api/entity/find/firstid"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        // State: entity 1 (unlabeled), A and B deleted
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("firstAvailableId is 1 (no labeled entities remain)", function() {
          expect(res.body).to.equal(1);
        });
