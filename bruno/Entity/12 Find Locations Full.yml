info:
  name: Find Locations Full
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/api/entity/find/locations/full"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Returns empty array (no named locations)", function() {
          expect(res.body).to.deep.equal([]);
        });
