info:
  name: 20 Check First Available ID
  type: http
  seq: 20

http:
  method: GET
  url: "{{baseUrl}}/api/entity/find/firstid"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        // State: entity 1 (unlabeled), A (labeled), B (labeled)
        // firstAvailableId = 1 — slot 1 is not labeled
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("firstAvailableId is 1 (entity 1 is unlabeled)", function() {
          expect(res.body).to.equal(1);
        });
