info:
  name: 14 Get First Labeled ID
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/entity/find/firstid"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("First ID is 1", function() {
          expect(res.body).to.equal(1);
        });
