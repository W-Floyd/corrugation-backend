info:
  name: 24 Delete Labeled Entity B
  type: http
  seq: 24

http:
  method: DELETE
  url: "{{baseUrl}}/api/entity/{{labeledEntityIdB}}"
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
