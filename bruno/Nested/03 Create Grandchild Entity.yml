info:
  name: Create Grandchild Entity
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/entity"
  headers:
    - name: content-type
      value: application/json
    - name: accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Grandchild Box",
        "description": "Nested inside Child Box",
        "location": {{childId}},
        "metadata": {
          "quantity": 1,
          "owners": [],
          "tags": ["grandchild"],
          "islabeled": false
        }
      }
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status === 200) {
          bru.setVar("grandchildId", res.body);
        }
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
        test("Returns grandchild entity ID as string", function() {
          expect(res.body).to.be.a("string");
        });
