info:
  name: Delete Child
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{baseUrl}}/api/entity/{{childId}}"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Returns 200", function() {
          expect(res.status).to.equal(200);
        });
