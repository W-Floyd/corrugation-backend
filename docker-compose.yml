version: "3"
services:
    corrugation-backend:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "8083:8083"
        restart: "no"
        volumes:
            - corrugation-data:/data

volumes:
    corrugation-data: